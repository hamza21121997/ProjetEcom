{% extends 'base.html.twig' %}

{% block title %}Sousscategorie{% endblock %}

{% block body %}
    <h1>Sousscategorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sousscategorie.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ sousscategorie.designation }}</td>
            </tr>
            <tr>
                <th>Idsouscat</th>
                <td>{{ sousscategorie.idsouscat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sousscategorie_index') }}">back to list</a>

    <a href="{{ path('sousscategorie_edit', {'id': sousscategorie.id}) }}">edit</a>

    {{ include('sousscategorie/_delete_form.html.twig') }}
{% endblock %}
